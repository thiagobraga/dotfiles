#!/bin/bash

# This script runs early to display the detected system environment.
source "{{ .chezmoi.sourceDir }}/.helpers/utils.sh"

info "========================================"
info "thiagobraga dotfiles"
info "========================================"
echo
info "  isLinux:   $(status {{ .isLinux }})"
info "  isUbuntu:  $(status {{ .isUbuntu }})"
info "  isDebian:  $(status {{ .isDebian }})"
info "  isWsl:     $(status {{ .isWsl }})"
info "  isDesktop: $(status {{ .isDesktop }})"
info "  isServer:  $(status {{ .isServer }})"
echo
info "========================================"
echo
