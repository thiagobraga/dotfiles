#!/bin/bash

{{ if and .isLinux .hasAVX .isFull }}

if ! command -v bun &> /dev/null; then
  info "Installing Bun..."
  curl -fsSL https://bun.sh/install | bash &>/dev/null
else
  info "Bun is already installed. Updating..."
  bun upgrade &>/dev/null
fi

{{ end }}
