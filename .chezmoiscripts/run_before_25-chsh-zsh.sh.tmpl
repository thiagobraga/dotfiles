#!/bin/bash

{{ if .isLinux }}
source "{{ .chezmoi.sourceDir }}/.helpers/utils.sh"
info "Setting zsh as default shell..."

sudo chsh -s "$(which zsh)" "{{ .chezmoi.username }}"

success "zsh set as the default shell"
{{ end }}
